# Минимальные зависимости для статических HTML файлов
# Никаких сложных пакетов не требуется

# Для HTTP сервера (если используется)
http-server>=14.0.0

# Для nginx (если используется Docker)
# nginx уже включен в Dockerfile.static
