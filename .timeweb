# Timeweb Cloud Configuration
# Подстраиваемся под их систему сборки

# Тип развертывания - используем их встроенный процесс
deploy_type: build-from-source

# Команды сборки - простые и надежные
build:
  commands:
    - echo "🚀 Начинаем сборку для Timeweb Cloud"
    - echo "📁 Проверяем структуру проекта"
    - ls -la
    - echo "🌸 Проверяем папку flower-shop"
    - ls -la flower-shop/
    - echo "✅ Статические HTML файлы готовы"

# Команды запуска - используем их встроенный сервер
run:
  commands:
    - echo "🌐 Запускаем сервер Timeweb Cloud"
    - echo "📱 Сайт будет доступен по вашему домену"
  port: 3000

# Переменные окружения
environment:
  NODE_ENV: production
  PORT: 3000
  TIMEWEB_MODE: static-html

# Рабочая директория
workdir: .

# Файлы для копирования
copy_files:
  - flower-shop/
  - .timeweb
